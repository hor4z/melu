// El taller vive en `Taller.tsx`. Acá sólo queda el punto de entrada, para que el módulo que Vite
// carga primero no exporte nada más que eso: un export de más rompe el Fast Refresh.

import './Taller.tsx'
