.env
/bin/
/data/
node_modules/
# el build del front vive acá adentro para que el binario lo embeba;
# el marcador queda para que `go build` funcione sin haber compilado el front
/packages/api/internal/web/dist/*
!/packages/api/internal/web/dist/.gitkeep

# Los reportes de la capa de tests del navegador: se miran cuando falla algo y no se versionan.
playwright-report/
test-results/
blob-report/
